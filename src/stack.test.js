import Stack from './stack';

describe('capacity', () => {
  test('アトデカク', () => {
  });
});

describe('size', () => {
  test('アトデカク', () => {
  });
});

describe('push', () => {
  test('アトデカク', () => {
  });
});

describe('pop', () => {
  test('アトデカク', () => {
  });
});
